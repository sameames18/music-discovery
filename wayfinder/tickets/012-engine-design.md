---
id: 012
title: Engine v1 design
label: wayfinder:grilling
status: open
assignee: none
blocked_by: [011, 009, 010]
---

## Question

With the prototype's lessons and both Score formulas settled: specify the v1 Engine. Which Signals it consumes (Tags by Axis, Critic Score, Community Score/Proxy, Participants overlap), how a Taste Profile is represented, the similarity/ranking function and its weights, how Critic vs Community acclaim trade off (a switch? a slider? fixed?), determinism guarantees (same Library + same Catalog snapshot = same list; what invalidates it), the Reason template and what it must cite, how Reactions feed back (per-Member suppression? weight adjustment?), how "show me more" extends the list without reshuffling, and the seams that let Influence and future Signals plug in without a rewrite.

This is the intellectual core of the product; take the session it needs.

## Resolution

(open)
