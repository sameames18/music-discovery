---
id: 017
title: Tech stack, hosting, and deployment
label: wayfinder:grilling
status: open
assignee: none
blocked_by: [007, 006]
---

## Question

Sam's constraints: one backend language, one frontend language, one database, one deployment method; $50–100/month is acceptable once there's real usage; agents write nearly all the code, Sam reviews. Given the Catalog's size from ticket 007 and any legal/hosting-region constraints from ticket 006: choose the backend language and framework, the frontend language and framework (or one language for both), Postgres hosting, the host and deployment method (one container on one VPS? a PaaS?), magic-link email delivery provider, background jobs (ingestion, score recomputation) without adding a second deployable, and the local dev setup an agent can bootstrap in one command. Include an estimated monthly bill at zero users and at "real usage".

## Resolution

(open)
